apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-image-updater-auth
  namespace: argocd
data:
  ecr-login.sh: |
    #!/bin/sh
    set -eu
    PASSWORD="$(aws ecr get-login-password --region eu-central-1)"
    echo "AWS:${PASSWORD}"
